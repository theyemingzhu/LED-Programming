{
  "name": "lightweaver",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --headed --debug",
    "firmware:waiting": "node scripts/firmware-queue.mjs",
    "firmware:park": "node scripts/firmware-queue.mjs add",
    "firmware:release": "node scripts/firmware-queue.mjs release",
    "bench": "node scripts/bench-check.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
