# Firmware images and signed release fixtures must retain their committed bytes
# on Windows as well as Unix. Never repair signature failures by normalizing data.
*.bin -text
release/test-vectors/** -text
lightweaver/public/firmware/** -text
release/job-sources/** -text
lightweaver/public/production/jobs/** -text
