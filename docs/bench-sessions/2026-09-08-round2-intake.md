# Round 2 bench intake, 2026-09-08

Card: lw-b0fe81f61b44. Firmware 1548 (build 1548, bridgeVersion 6). Station 192.168.18.70 / lightweaver.local. Project lightweaver-bench-discovery-v1, 41 px GPIO 18. Studio live build 1650 (origin/main 1652; the two-commit gap is docs-only, no site change). Chrome 152 on macOS. Adrian present, one observation at a time. Branch round2/card-journey.

## Defects

**F16 · fixed on branch** · Lights off while Studio says Connected. Screen: Patterns, footer Connected, Tune Brightness 100%. Expected: the strip plays the selected look. Observed: strip dark. Root cause: card zone reports blackout true (a Stop sent earlier); Studio surfaces blackout only inside CardControlDrawer, never on Patterns or Card Home. Confirmed by Adrian: tapping a pattern on the card's own page lit the strip. Fixed on the branch (commit "fix: F16 - surface a card-side blackout and make Recover lights the primary action"): the shared journey evidence reads /api/zones, Card Home and Patterns show "Lights are off on the card." with Recover lights as the one action, gated off provisional and not-ready cards. Regression [J16-blackout] in tests/journey-continuity.spec.ts; conductor re-ran: journey 10/0, unit 2392/2392, mutation (blackout fact stubbed false) turns J16 red.

**F17 · fixed on branch** · Pattern clicks in Studio never reach the card; toast "The card page opened but did not answer". Screen: Patterns. Expected: strip changes on click. Observed: pop-up card window opens, Studio times out after 10 s. Root cause (proven in a real browser against the real card): Studio reopens the pop-up by window name with a URL whose only variable part is the fragment, so an already-open pop-up is focused, never re-executed, and never re-posts ready; Studio then wipes its own verified bridge state and waits. The halfway nudge meant to re-navigate the window was gated on a reservation that no code ever set. Excluded with evidence: origin mismatch, 512-char fragment cap, allowed-keys rejection, version gating. Fixed on the branch (commit "fix: F17 - reopen the card bridge with a fresh URL so a reused pop-up answers again"): a per-attempt token in the query string; unit 2390/2390; test:core green; mutation (token back in the fragment) turns 3 tests red. Regression: src/lib/cardBridge.openLocalCardPage.test.js.

**F18 · fixed on branch** · Edit in Studio from the card pop-up opens a new Studio tab at Setup phase 1 ("wiring has changed in Studio since it was installed", primary "Use this card's project") instead of returning to the open Studio tab on Patterns. Screen: card pop-up, then new Studio tab at ?cardBridge=1&cardHost=lightweaver.local&editLook=combo-aurora#screen=card&section=overview with "Restored from recovery copy". Expected: back in the existing Studio tab, Patterns, Aurora selected. Root cause: firmware 1548's Edit in Studio navigates the opener Studio tab to a new URL (a full reload), so in-memory state is lost and the autosaved copy is restored; that copy carries the owner's unsaved wiring edit, so the resolved-adoption run honestly refuses and the phase-1 ladder is the only screen left. The intent then reached Patterns, which bounced back to the card screen when it could not claim the exact-fingerprint edit authorization. Fixed on the branch (commit "fix: F18 - an edit request from the card page lands on Patterns and stays there, never the setup ladder"): Card Home routes a same-project edit intent to Patterns after the adoption refusal; Patterns stays put and shows its own "Verify project in Card status" step; the look is offered, not auto-selected (write gates untouched). Regression tests/journey-edit-intent.spec.ts [J18-edit-intent] and [J18-bare]; conductor re-ran 2/0. Firmware half (message the open Studio tab instead of reloading it) waits on Adrian's release call.

**F19 · split: F17-A queued, firmware part pending Adrian's release call** · The pop-up exists at all in Chrome, and opens too small to show its content. Root cause, part 1 (Studio): Patterns forces the bridge whenever the page is https (lw-pattern.jsx needsBridge / preferBridge; cardConnection.js canPushDirectlyToCard returns false on https). Measured from the live Studio origin: direct GET and a POST preflight to the card both succeed with private-network CORS, so the premise is false. CardControlDrawer already does not force the bridge. Part 2 (firmware skew): the page firmware 1548 serves lacks the bridge-utility element that the current source has, so the compact utility view fails and the full card page renders inside a 360x180 window; fixed in firmware source, needs a signed release and a flash. Studio part fixed on the branch (commit "fix: F17-A - Patterns reaches the card directly on https and opens the pop-up only when the direct probe fails"): Patterns probes connectCardTransport first and opens the pop-up only when the probe fails; regression tests/journey-direct-preview.spec.ts [J17-direct] and [J17-direct-fallback]; conductor re-ran 2/0, mutation (helper forced to bridge) turns J17-direct red. Not yet proven on Chrome 152 against the real card, where a Local Network Access prompt may appear; that is the bench click. Firmware part waits on Adrian's release call.

## Notes

tests/live-bridge-handshake.spec.ts covers F17 but runs in no CI lane and needs hardware, so it cannot gate; the regression that gates is in src/lib/cardBridge.openLocalCardPage.test.js (test:unit). Chrome 152 is rolling out a Local Network Access prompt, so the direct path must probe and fall back rather than hard-switch.

**F20 · passed on bench (Studio 1662)** · First tap after a page load shows "That tap was not sent to the card. This card is not ready for pattern commands." and sends nothing. Screen: Patterns on Studio 1659, footer Connected. Expected: the tap goes to the card. Observed (Adrian, Chrome 152): toast, strip unchanged, pop-up only later. Root cause (from code, not captured live): the readiness gate in scheduleBrowseLivePreview reads a cardLink.readiness snapshot that has not absorbed the direct probe that just succeeded. Status: the readiness gate now accepts a just-succeeded direct probe for that tap (same commit as F21), but no test turns red without it: forcing directProbeReady false left [J21-first-tap] green, so this half is unproven and the suite does not cover it. Proof is Adrian's next first tap; if the toast recurs, F20 gets a live capture and its own red test.

**F21 · passed on bench (Studio 1662)** · First tap after a page load forces the pop-up without probing the card. Measured live on Studio 1659 in a real browser: a tap 466 ms after navigation called window.open 3.4 ms later with no /api/status probe in between; a tap after 30 s went direct (status, zones, control all 200, no pop-up). Root cause: resolveCardBridgePreference returns "bridge" when expectedCardId is empty, and cardLink is still its initial empty state until the first status poll lands; once a bridge is verified, transport === 'bridge' locks later taps onto the pop-up. Fixed (commit "fix: F20/F21 - the first tap after a page load probes the card directly instead of forcing the pop-up or a not-ready toast"): while the link is still bootstrapping the probe uses the persisted card identity; only a genuinely absent identity forces the pop-up. Regression [J21-first-tap] in tests/journey-direct-preview.spec.ts with a delayed first status answer; conductor re-ran 3/0 and confirmed the mutation (force the pop-up on empty identity) goes red.

Shipped 2026-09-08: Studio build 1659 (sourceRevision 226d0375), firmware unchanged at 1548. Bench result on 1659: F17 confirmed (pop-up answers, taps work through it); F17-A not yet effective on the first tap (F21); F16 and F18 not yet exercised by Adrian.

**F22 · fixed on branch** · Lights off on the card page, Studio shows nothing. Screen: Patterns on Studio 1662, footer Connected; Adrian pressed Off on the card's own page. Expected: "Lights are off on the card." with Recover lights. Observed: no message, Recover lights not found. Root cause: the F16 gate also required an exact wiring match (matchesOpenProject or saved-match); Adrian's open project has the card's project id with an unsaved wiring edit, so a real blackout was hidden. Fixed: blackout is reported on project-id equality (same fact F18 uses); Recover lights on Patterns is enabled in that state and (F22b) restores the lights without a pattern edit authorization. Regressions [J22-blackout-drifted] and [J22b-patterns-recover]; conductor re-ran journey-continuity 12/0 and confirmed restoring the strict gate turns both J22 tests red.

**F23 · built, parked in the firmware queue** · Card page: the blackout button always read "Off" so it did not read as a toggle; Edit in Studio reloads the opener Studio tab. Fixed in firmware source on branch round2/fw-card-page (bridge v7): the button reads "Lights off" / "Lights on" by state with aria-pressed, and Edit in Studio posts an open-studio message to the live opener at the allow-listed origin, falling back to navigation. Compiles (esp32-s3-n16r8 SUCCESS); firmware contract tests green. Not merged: a firmware source change triggers the signer, and Adrian deferred the release. Queue entry landed on main (PR #240); drain with npm run firmware:release when he says so.

**F24 · fixed on branch** · Studio side of F23: cardBridge handles open-studio from the verified bridge window only, applies the intent in place (replaceState, hash to Patterns, focus), no reload. Inert on firmware 1548. [J24-open-studio] and [J24-open-studio-ignored]; unit 2399/2399.

**F22c · fixed on branch** · Main went red on the F22 merge (PR #241): CI browser smoke failed [J22-blackout-drifted] three times identically on the Patterns assertion while the same lane was green on the idle Mac. Reproduced on the Mac under 8-core CPU load (with-cpu-load.sh), where [J22b] failed at the same assertion; the trace showed one /api/zones read and then only status polls. Root cause: when Patterns' journey refresh is the FIRST evidence read for a card and boot (a hash navigation from Card Home can outrun Card Home's own read under load), it published projectId from the empty previous snapshot, so the blackout fact was tagged with no project and the same-project gate rejected it. Fixed: the refresh carries the open project id. Regressions: three unit tests on the evidence store; conductor re-ran under load J16+J22 3/0 green and, with the two source files reverted, J22b red again.

Shipped 2026-09-08: Studio build 1662 (sourceRevision 772f65ca), firmware unchanged at 1548. Bench result on 1662, Adrian, Chrome 152, hard reload then a tap within the first second: strip changed right away, no pop-up, no "not ready" message. That is the hardware proof for F17-A (direct path on https), F20 and F21. F19 Studio half therefore holds in Chrome; the pop-up now appears only where the direct probe fails (Safari/iOS untested). F16 and F18 still not exercised on the bench.

## Ledger

No rows flipped; no hardware proof passed from a mock. No card write, flash or deploy performed.

## Overnight loop, 2026-09-09 (Adrian asleep; Fable conducting, agents doing)

Shipped: Studio build 1676 (sourceRevision 1e701178), firmware 1548. Owner walk W1 on 1676 by an agent in a fresh Chromium profile on the card's Wi-Fi, desktop 1440x900 and phone 390x844, no card writes beyond previews and blackout on/off. Findings, ranked:

**W1-1 · open, critical** · Studio flips between "Connected · Card firmware 1548" and "Not connected · Card firmware unknown" across screens and reloads with no user action; Connection Center reads "Card verified - This exact card answered, but Studio is still verifying its installed project before ordinary controls are enabled" for 18-20 s while the card's own /api/status answers instantly with runtimePhase ready. Blocks Patterns "Open card page" ("not ready for pattern commands"), Playlist Live (silent), and Card Home Recover lights on the phone ("The card bridge transport is open, but card identity is not verified", no recovery request sent). Root cause (deep model, real browser, live site, 90 status envelopes, same boot id throughout, card answering in 72 ms): two separate faults. (a) The state machine is not stuck; the lifecycle state is project-mismatch, a terminal verdict, and CardConnectionCenter prints "still verifying its installed project" for every state that is not the literal ready (safeControlAccess), beside a heading gated on transport only, so one panel says "Card verified" and "still verifying" at once; the footer chip read "Save to card", which is the project-mismatch label, not the checking label. The mismatch itself: a fresh browser has no installation record, so the fingerprint conjunct fails on every poll while the project id matches. (b) The footer flip is a bridge keepalive timeout: one missed 2.5 s ping demotes to reconnecting-bridge although the miss limit is 2, and a backgrounded pop-up gets throttled by Chrome while Studio's own tab is visible, so the visibility guard never fires. Excluded by measurement: stale boot id, hanging adoption, direct-vs-bridge authority flip (killing the bridge failed over to direct in 1.2 s). Tickets: F25 (copy on project-mismatch, in progress), F26 (miss limit, fixed in batch 1: one missed keepalive tolerated, second consecutive miss demotes, explicit identity errors never tolerated; conductor updated the core-lane fixture and re-ran unit 2410/2410, core lane exit 0).
**W1-2 · open, high** · Recover lights on Card Home did not clear a real blackout; consequence of W1-1. The conductor restored the card with the same request the card page sends (POST /api/control blackout false).
**W1-3 · hardening in progress (F27), not reproduced** · "Use this card's project" (phase 1 primary) was a silent no-op 2 of 3 times on a fresh hard reload (no network, no status, lifecycle record unchanged); worked the 3rd time.
**W1-4 · open, medium** · A completed setup (phase 4, installed project matches) reverted to phase 1 "Project not installed" after a connection hiccup, card untouched. Likely W1-1.
**W1-5 · fixed, batch 1 (F28)** · "Recover and verify card" then "Find my card" navigated the SAME tab to the card page bridge URL and stranded it (history.back a no-op).
**W1-6 · closed, fixed in 1701** · Card Home "Checks & recovery": Verify hardware and Color-order test do nothing (no request, no status, no hash change, 3 attempts); Recover lights in the same panel works. Agent traced both handlers end to end and drove them with the simulator in the walk's exact seed (fresh browser, no lifecycle record, card holding a project, both matching and mismatching): 4/4 green, no guard found; tests/card-home-checks.spec.ts added (mutation: neutered handlers go red). One unproven lead: these three handlers are the only card-reaching calls that never forward the link transport, which on the https site could route them wrong; Adrian started a separate session to check that against the real card. Not fixed: a symptom that does not reproduce is not fixed, only guessed at. **Closed 2026-09-09 (PR #245 + #247, Studio 1701):** the separate session reproduced the lead against the real card (lw-b0fe81f61b44, firmware 1548) on the live site at 1683 from Adrian's own browser profile: that profile allows the plain-http card fetch from the https page, so the link was connected-direct ("Connected on local network", no bridge tab), and Verify hardware / Recover lights each showed a bridge error ("Open the card page once…", "card identity is not verified") because the three handlers routed by page protocol instead of the link. Color-order test navigated and scrolled correctly. The exact reported symptom (no status line at all, Recover lights working) did not reproduce; every click produced a status line. Fix: the three calls forward the link transport, with recoverCardLights, its restart path and the wiring-safety reads honouring it; unit tests go red without it, and the https lane of card-home-checks.spec.ts now runs on every PR. Not measurable headlessly at 1772: a fresh browser over the bridge stops at "Found — pair" (needs the physical press), so the panel is only reachable from a paired browser.
**W1-7 · open, low** · Patterns renders blank for 1.5-2 s after a hard reload with no loading state.
**W1-8 · open, low** · Pattern bank header "134 shown of 132 chip-ready" (shown exceeds total).
**W1-9 · open, low** · Duplicate pattern names in the pool (Digital Rain, Lava Lamp, Watercolor Wash).
Clean: build proof, no horizontal overflow at either size, single and rapid-triple chip taps push live preview, hue/speed sliders push, Layout's wiring-lock copy, Show and Lab load, no JS exceptions (failures are silent, not crashes). Screenshots could not be captured (CDP screenshot timed out every time in that browser); evidence is verbatim text, DOM state and network traces.

### UX critique U1, Card Home and the Setup ladder (agent, simulator screenshots at 1440 and 390, four states)

SAFE, building tonight: (1) with the strip blacked out, the ready banner's "Open Patterns" is a second orange primary beside "Recover lights"; the banner yields while blackout is shown. (2) The hero heading always reads "Set up your Lightweaver" even two lines above "Setup complete"; when setup is complete it reads "Your Lightweaver". (4) "Restored from recovery copy" is shown as a persistent badge AND a toast at once; the toast goes. (5) On the phone the Installed field clips to "Same project, not yet verif…"; values wrap instead. (7) Phase 4's summary table repeats Card and Project directly under the identity row that already says them; those two rows go.

NEEDS ADRIAN (morning): (3) blank card: the ladder's "Find and count the lights" and the Detected-state panel's "Find my strips" start the same flow from two boxes; fold the "load a project instead" offer into the ladder task and retire the second box. (6) right after Studio silently adopts a card's project, CARD reads "Matrix piece" while PROJECT reads "Untitled Project" under "Installed project matches"; carry the card's piece name into the adopted project.

Must stay: the blackout banner (one sentence, one action), the four-field identity row, folds closed by default, one active numbered ladder task, one-fact footer chip. Contradiction to name later: two buttons both called "Recover lights" with different remedies (blackout banner vs Checks & recovery fold).

### Load harvest W2 (agent, 13 runs: one idle control, four spec sets x3 under 8-core CPU load)

Clean under load: all ten journey specs (30/30 x4), card-state-matrix (65 + 5 known fixme x3), patterns-v3, card-workspace, setup-ladder, studio-route, notice-layer, strip-discovery (40/40 x3). Every failure sat in playlist-storage.spec.ts:
**F29 (A, product race) · open** · "Playlist serializes card mutations behind recovery": three real /api/recover-lights POSTs reached the simulated card where the contract intends two; the third came 683 ms after the second, the redeliver retry's 500 ms plus one round trip. Cause: redeliverRecoveryAfterRestart caps each attempt at min(timeout, 1200 ms) (cardLiveControl.js ~514, 519), so a successful round trip that exceeds 1.2 s of main-thread delay is treated as failed and the recovery is sent again, a duplicate physical write. Fix: use the same 3000 ms budget as the rest of the recovery path.
**W2-B1 (test race)** · "marks a row runtime-applied only after the card acknowledges": the test asserts an intermediate "Sending to Lightweaver" render that a 240 ms click delivery under load skips past (mock write latency 120 ms). Fix the assertion, product correct.
**W2-B2 (deterministic test defect, fails idle too)** · "overflow blocks clipboard, blob, download side effects": reads childNodes[0].textContent of the notice wrapper, which concatenates the action label ("...try again.Open card page"). Read .lw-notice-copy. Means this copy has never actually been verified.
Note for future harvests: each Playwright invocation wipes test-results/, so failing specs must be re-run in isolation right after a battery to keep traces.

### UX critique U2, Patterns and Playlist (agent, simulator screenshots at 1440 and 390; host load 26-34 kept the simulated link from settling, so blackout and playing states were not captured and two unsettled shots reproduce W1-1)

Full report: docs/ux/2026-09-09-u2-patterns-playlist-critique.md. SAFE, building: (1) one vocabulary for "has this reached the card" (Patterns bank pill, Design target card and Playlist each use a different three-word set); (2) a phone CSS rule written for Patterns' preview also floats Playlist's Saved looks and Pattern pool above the playlist itself; (3) the bank header count "134 shown of 132 chip-ready" divides all patterns by chip-ready only, and a correct count sits one line below; (4) the "not ready" refusal notice floats over the Design target card and hides Pixels driven and Save look; (5) "Recover lights" and "Repair LED" are one function under two names, and the prominent one disappears in exactly the uncertain states; (6) "On the card now" is a per-tap value on Patterns and a panel title on Playlist; (8) dead menu wrapper around Copy chip config. NEEDS ADRIAN: (7) the footer can read "Card restarted, verifying" while every tile looks live and tappable; the screen should visibly soften while the footer is not Connected, a product decision about how long verifying may last and what pending looks like.

### Overnight loop, what actually ran

Active from 23:40 to 01:50. Shipped batch 1 (Studio 1683: F26, F28). Built and verified but unmerged at 01:50: F29, F25, F27, U1 compact pass (batch 2). At 01:50 the batch-2 browser run was rejected in the conductor's window and the loop stopped waiting for Adrian until 07:xx: about five idle hours. One agent (W1-6) sat six hours waiting on its own test monitors before reporting. Lessons for the next loop: a single tool rejection must not stall the loop (retry after a wakeup, or continue with the other streams), and every agent brief carries a runtime cap.

Shipped 2026-09-09: Studio build 1683 (batch 1, PR #243: F26, F28) and Studio build 1701 (batch 2, PR #244: F29, F25, F27, U1 Card Home compact pass, plus PR #246 from a sibling session fixing the last two recovery-toast assertions and PR #247 ci ordering). Firmware unchanged at 1548. Batch 2's first Tests run went red on the cloud lane (a spec asserting the retired toast, in a lane PRs do not run); the sibling session's fix landed before the conductor's duplicate did. Live proof: sourceRevision 6287f514, buildNumber 1701. Still unmerged: W1-6 test-only coverage (round2/w16-dead-checks fec8be33), U2 Patterns/Playlist compact pass (in progress).

Shipped 2026-09-09: Studio build 1708 (batch 3, PR #249: U2 Patterns/Playlist compact pass). Firmware 1548. Live proof: sourceRevision 0ae2779d, buildNumber 1708.

### Walk W3 on build 1701 (agent, fresh profile, real card; 19 minutes; pattern-chip taps confounded by a CDP fault and not reported)

Gone since 1676: W1-5 (Find my card opens a pop-up, Studio's tab untouched), the "still verifying" copy (Connection area now reads "This card holds a different project from the one open in Studio." with "Use this card's project"), the Card Home compact items (heading "Your Lightweaver", no duplicate toast, phase 4 no longer repeats Card/Project). Not reproduced: W1-1 flip over 24 s, W1-3 (n=1). Not walked: W1-4, W1-7.

**F30 · fixed on branch** · A blackout switched on from the card's own page never reaches Studio: Card Home stayed on "Card ready" for 18 s and Patterns showed no notice, while Recover lights on Patterns did clear the blackout when pressed. Cause: the journey evidence (including the zones read) is fetched once per card and boot and only invalidated by Studio's own hardware operations; an out-of-band press on the card page is never seen. This is exactly what Adrian did on 2026-09-08 and why he could not find Recover lights; F16/F22 fixed the gate but not the refresh. Fixed (commit "fix: F30 - a blackout switched on the card's own page reaches Studio within one status poll"): when the polled status envelope's output brightness bytes change between polls, the cached journey evidence is invalidated and zones are re-read; zones stay the source of truth. Regression [J30-blackout-out-of-band] with a harness method that flips the simulated card out of band: red at HEAD (times out), green in about 12 s, mutation (invalidation removed) red, loaded run J30+J16+J22 4/0.
**W1-8** still visible on 1701, as expected (the U2 fix shipped after the walk, in 1708).
**W1-9b · open, low** · a mix named "Aurora" sits beside the pattern "Aurora" (positions 02 and 03); a mix should carry its full name or a "mix" mark that reads at a glance.
**W1-6 · closed** · see the W1 walk entry above: reproduced from Adrian's paired browser at 1683 (link direct, handlers guessing bridge), fixed in 1701. The walker's "nothing within 3 s" at 1701 came from a session whose click tool was faulting and is superseded by that measurement. If Adrian wants a last human confirmation: open Card Home, Checks & recovery, press Verify hardware; a readback line names the card and its LED count.
**W1-6** · Verify hardware showed nothing within 3 s on the real card at 1701, which already carries the sibling session's routing fix; the walker could not see network traffic and its click tool was faulting in that session, so this is unproven either way. Adrian's click: open Card Home, Checks & recovery, press Verify hardware; does a readback line appear?

Shipped 2026-09-09: Studio build 1739 (F30, PR #254). Sibling sessions released firmware 1759 (VERSION 1.1.34/1.1.35), which drained the queue and carries F23 (card page: Lights on/off label, Edit in Studio messages the opener, bridge v7, readable pop-up utility view). Adrian's card still 1548 until he runs Update card.

**F30 · passed on bench (Studio 1767)** · Adrian pressed Off on the card page; Studio showed "Lights are off on the card." on Patterns (as a floating notice, half under the Connect dialog) and on Card Home. His ask: "find better place to say it" (F32, in progress: the footer connection chip reads "Connected · Lights off", one click recovers; the floating notice goes) and "make it toggle switch" (F23c, firmware: the card page control is a role=switch with Lights On/Off; built, compiles, parked in the queue for the next release, PR #260).
**F33 · shipped, Studio build 1772 (PR #261)** · Pressing Recover lights on Card Home showed "The card did not answer before the preview request timed out." Reproduced on the real card from the live site: no request reached the card; the banner's handler built its own options without the link's transport and guessed the pop-up bridge (the same gap W1-6's sibling fix closed for the Checks panel). Fixed by one shared options builder; [J33-card-home-recover-https] red at HEAD, green with the fix, mutation red; unit 2442/2442. Follow-up: Patterns' repairLed has the same gap (F33b).

**F32 · merged (PR #262), deploying** · "Lights are off on the card." now lives in the footer connection chip on every screen as "Connected · Lights off" (amber dot); one click on the chip recovers through the same request Card Home's banner sends; the floating Patterns notice is gone; Card Home keeps its in-flow banner; the toolbar Recover lights stays primary while dark. Deliberate precedence: while the card also needs a save (drifted wiring) the chip shows that instead, and the banner and toolbar carry the blackout. J16/J30 red without the chip branch, green with it; unit 2442/2442.
**F33b · in progress** · Patterns' toolbar Recover lights has the same missing-transport gap as F33; shared options builder.

Shipped 2026-09-09: Studio build 1777 (F32, PR #262). F33b parked with a resume entry (TODO ## Follow-ups), then picked up by F36.

**F34/F35 · fixed, batch 4** · Adrian pressed "Update card" on Card Home (card 1548, release 1759 published, footer "Connected"): the install screen said "Studio can't reach the Lightweaver card it remembers" and showed the USB erase installer ("Either way this erases the card's Wi-Fi, its piece and its settings", "Find connected card") instead of the preserving Wi-Fi update panel. His words: "I'm often on the wrong doors. And you have no control on which the right doors are. When I open it up and click card, I should always be on the right door." Cause family: the install screen's own reachability check builds its request without the link's transport (same as F33, W1-6). Tickets: F35 (the check reads the link), F34 (one door: while the link is Connected the screen opens on the Wi-Fi update; USB erase only from Hardware, labelled Erase), F36 (sweep every card-reaching call onto the shared options builder plus a source guard so the family cannot return).

Correction to F34/F35's cause: the install screen was not guessing the transport; it hid the Wi-Fi door on the card's live `firmwareUpdateReady` flag (a "safe to write this instant" bit) and, with no door chosen, its remembered-card timer printed "can't reach" over a card the footer called Connected. Fixed: the door is gated on capability plus the link Studio holds; the USB erase form is not rendered while connected. [F34-one-door] and [F35-reach] red at HEAD, green, mutation (connectedness dropped) red.
**F36 · fixed, batch 4** · Sweep: sixteen more card-reaching call sites across Patterns, Playlist, Settings, Setup, the control drawer, strip discovery, colour-order check, the push control and the footer chip now forward the link's transport through one builder in src/lib/cardConnection.js; a source guard in cardConnection.test.js fails the moment a new call mentions host without transport (proved red with one bare call). Two more fixture defects fixed while finishing F33b's test (a strip fixture crashed Patterns; the blackout wait raced the 20 s direct keepalive). journey-continuity 15/15.
**F37 · finding, firmware** · The real card reports firmwareUpdateReady false while healthy because its LittleFS project-repository partition was never formatted (factory-flashed before the subsystem existed) and begin() never formats; the Wi-Fi update path refuses with a generic 409 and the reason only reaches Serial. Owner's safe path to 1759 today: the one-time USB update (keeps Wi-Fi, project, settings; talks to the ROM bootloader, not the refused API). Firmware fix F38 in progress: format on a first-time mount failure and carry the reason in the status envelope and the refusal detail; parked for the next release.

Shipped 2026-09-09: Studio build 1792 (batch 4, PR #267: F34/F35 one door, F36 transport sweep with source guard, F33b). Adrian: "When this is finished, let's ship everything out." Firmware release drained from the queue (PR #268, VERSION 1.1.36): the card page lights switch (F23c) and the storage self-repair with refusal reasons (F38). Signer running at the time of writing. Adrian's card (1548) takes this release by the one-time USB update in Chrome (its own Wi-Fi path is refused until F38 is on it); every later update goes over Wi-Fi.

Open for Adrian's eye (from the UX critiques, not built): U1-3 fold the blank card's "Find my strips" box into the ladder task; U1-6 carry the card's piece name into a silently adopted project; U2-7 soften Patterns while the footer is not Connected. Also W1-9b (a mix named "Aurora" beside the pattern "Aurora").

Follow-ups filed by agents, not built: Studio's refusal path reads result.message but the card answers {error, detail} (cardFirmwareUpdater.js ~298), so the new projectRepositoryMessage never reaches the owner; readCardZonesFromCard / readCardPatternsFromCard drop options.transport before connectCardTransport when no authority exists; CardPushControl's "local-only" direct push runs on https too; cardWiringSafety's getCardWiringStatus still guesses from page protocol.

Shipped 2026-09-09: firmware release 1.1.36, card build 1798 (signer commit 37f42ef8, PR #268: F23c lights switch, F38 storage self-repair and refusal reasons). Studio build 1801.

**F39 · shipped in batch 5 (self-reload half); tab lock parked on round2/f39b-tab-lock** · Adrian: "if I have a version open and then I do another version open, it sometimes causes problems in between everything." One Studio at a time: a superseded tab reloads itself when idle; a second tab shows "Studio is open in another tab." with one takeover button and the old tab goes quiet.
**F40 · shipped in batch 5** · Adrian could not find the one-time USB update: his card advertises Wi-Fi updates so the panel resolves to Wi-Fi mode and never offers USB, while the card refuses Wi-Fi (F37). The panel now offers "Update once over USB instead" and leads with it when the card reports firmwareUpdateReady false.
**F41 · shipped in batch 5 (cases a, c, d proven; b, Install on card as the one primary while changes are pending, still needs its test)** · Card Home with setup complete showed three orange buttons (Open Patterns in the release banner, Open Patterns in phase 4, Install on card). Adrian: "It confuses me of what to interact with." Decided: one primary at a time (Recover lights, then Install on card, then Update card, else none); Open Patterns leaves Card Home (the sidebar's job); the finished ladder folds to one line.

Note on the tab lock: its second-tab quieting broke the tested two-tab contract (F2 write lease, journey-ownership J08), so only the self-reload half ships; whether Studio should allow two live tabs is Adrian's call. Also learned: two agents in one worktree revert each other (memory: one agent per worktree). The offline-studio spec's two timeouts are red on main itself (control run 2026-09-10), not from F39.
